# Development environment variables (local only)
# This file is used for local development and testing

# Point to local backend for development
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel doesn't need these in dev, but good to have
NEXT_PUBLIC_SITE_URL=http://localhost:5000
